[README.md](https://github.com/user-attachments/files/24724594/README.md)
# Telegram MTProto + WhatsApp Proxy Control Panel

Production-ready установка **MTProto** и **WhatsApp Proxy** с управлением через **Telegram-бота**.  
Один bash-скрипт — установка с нуля, автозапуск, алерты и меню управления.

---

## 🚀 Возможности

### 🟦 MTProto (Telegram)
- Порт: **8443**
- Генерация нового SECRET
- Готовые proxy-ссылки (`tg://` и `https://`)
- Статус, аптайм, сетевой трафик
- Перезапуск контейнера

### 🟩 WhatsApp Proxy (официальный)
- Docker image: `facebook/whatsapp_proxy`
- Порт: **443**
- Статус, аптайм, трафик
- Ссылка для клиента (`IP:443`)
- Перезапуск контейнера

### 🤖 Telegram Bot
- Доступ **только ADMIN ID**
- Постоянная кнопка **🏠 Главное меню**
  - всегда видна у поля ввода
  - работает как `/start`
- Inline-меню управления
- Алерты в Telegram:
  - падение контейнера
  - восстановление
- Логирование ошибок бота

---

## 🧱 Архитектура

```
┌─────────────┐
│ Telegram    │
│ Admin       │
└─────┬───────┘
      │
      ▼
┌────────────────────────┐
│ Telegram Control Bot   │
│ python-telegram-bot v20│
└─────┬───────────┬──────┘
      │           │
      ▼           ▼
 MTProto       WhatsApp Proxy
 8443/TCP       443/TCP
```

---

## 📦 Требования

- Debian 11
- Root-доступ
- Свободный порт **443**
- Telegram Bot Token
- Telegram User ID (admin)

---

## ⚙️ Установка

```
chmod +x install_all.sh
./install_all.sh
```

Во время установки будет запрошено:
- `BOT TOKEN`
- `ADMIN TELEGRAM ID`

---

## 🧪 Проверка

```
systemctl status tg-proxy-bot
journalctl -u tg-proxy-bot -f
docker ps
```

---

## 📱 Использование

1. Открой чат с ботом
2. Отправь `/start`
3. Или нажми **🏠 Главное меню**
4. Управляй MTProto и WhatsApp Proxy через кнопки

---

## 🗑 Полное удаление

Удаляет **всё полностью** (бот, systemd, контейнеры, образы, логи):

```
systemctl stop tg-proxy-bot && \
systemctl disable tg-proxy-bot && \
rm -f /etc/systemd/system/tg-proxy-bot.service && \
systemctl daemon-reload && \
docker rm -f mtproto waproxy 2>/dev/null || true && \
docker rmi telegrammessenger/proxy facebook/whatsapp_proxy 2>/dev/null || true && \
rm -rf /opt/tg-proxy-bot /var/log/proxy-install.log
```

---

## ⚠️ Важные замечания

### WhatsApp Media
- Чат идёт через прокси
- Медиа может требовать прямой доступ клиента к `*.fbcdn.net`
- Это ограничение WhatsApp, не скрипта

### Порты
- MTProto: **8443**
- WhatsApp Proxy: **443**
- Менять не рекомендуется

---

## 🔒 Безопасность

- Ответы только ADMIN ID
- Используются официальные Docker-образы
- systemd + автоперезапуск

---

## 🛠 Стек

- Docker
- python-telegram-bot v20
- systemd
- Bash
- Telegram Bot API

---

## 📄 Лицензия

MIT

---

## ✅ Статус

✔ Production ready  
✔ Проверено на Debian 11  
✔ Установка одним скриптом
